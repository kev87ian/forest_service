{{#partial "content"}}
    <div class="jumbotron">
    <h3>All Animals </h3>
    {{if animals}}
        {{#each animals }}
            <div class="container" >

            <p>{{ name }} </p><br>
            </div>
        {{/each}}
    {{else}}
    </div>
{{/partial}}
{{>layout.hbs}}